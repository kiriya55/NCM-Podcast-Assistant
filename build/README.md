# Build 目录说明

此目录用于存放构建资源。

## 图标文件

如需自定义应用图标，请将 `icon.ico` 文件放置在此目录。

### 图标要求

- 格式：Windows ICO
- 尺寸：256x256 像素
- 建议包含多种尺寸（16x16, 32x32, 48x48, 256x256）

### 生成图标

可以使用在线工具（如 https://convertico.com/）将 PNG 图片转换为 ICO 格式。

## 注意事项

- 如果没有提供图标，构建时会使用默认图标
- 图标文件会在打包时嵌入到可执行文件中
